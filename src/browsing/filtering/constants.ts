export enum FilterOperations {
  equal = "eq",
  contains = "contains",
  in = "in",
  greaterThan = "gt",
  greaterThanOrEqual = "gte",
  lessThan = "lt",
  lessThanOrEqual = "lte",
}

export enum FilterOperators {
  and,
  or,
}
